export const metadata = {
  title: 'Activity',
  description: 'Platform usage trends — chats started, new registrations and documents uploaded over the last 14 days.',
  robots: { index: false, follow: false },
}

export default function Layout({ children }) {
  return children
}
