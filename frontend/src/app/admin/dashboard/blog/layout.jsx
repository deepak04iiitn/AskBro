export const metadata = {
  title: 'Blog — Admin',
  description: 'Create and manage AskBro Journal blog posts.',
  robots: { index: false, follow: false },
}

export default function Layout({ children }) {
  return children
}
