export const metadata = {
  title: 'Documents',
  description: 'Track document uploads, file types, storage usage per workspace.',
  robots: { index: false, follow: false },
}

export default function Layout({ children }) {
  return children
}
