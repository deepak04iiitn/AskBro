export const metadata = {
  title: 'Testimonials — Admin',
  description: 'Review and manage user-submitted testimonials.',
  robots: { index: false, follow: false },
}

export default function Layout({ children }) {
  return children
}
