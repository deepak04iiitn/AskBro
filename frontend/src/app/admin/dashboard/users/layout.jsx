export const metadata = {
  title: 'Users',
  description: 'View and manage all platform users, their roles, workspaces and join dates.',
  robots: { index: false, follow: false },
}

export default function Layout({ children }) {
  return children
}
