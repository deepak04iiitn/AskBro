export const metadata = {
  title: 'Workspaces',
  description: 'Monitor all workspaces — member counts, document counts, storage and chat activity.',
  robots: { index: false, follow: false },
}

export default function Layout({ children }) {
  return children
}
